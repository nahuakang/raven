name: CI
on: [push, pull_request, workflow_dispatch]

jobs:
  build_windows:
    name: Windows Build and Check
    runs-on: windows-2022
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - name: Set up Odin
        shell: cmd
        run: |
          git clone https://github.com/odin-lang/Odin.git --depth 1 --single-branch --branch=master
          cd Odin
          call "C:\Program Files\Microsoft Visual Studio\2022\Enterprise\VC\Auxiliary\Build\vcvars64.bat"
          ./build.bat
          ./odin version
          ./odin report

      - name: Check All
        shell: cmd
        run: |
          set FLAGS="-vet-unused -vet-using-stmt -vet-using-param -vet-style -vet-semicolon -debug"
          call "C:\Program Files\Microsoft Visual Studio\2022\Enterprise\VC\Auxiliary\Build\vcvars64.bat"
          .\Odin\odin check examples/hello %FLAGS%
          .\Odin\odin check examples/hello %FLAGS% -define:GPU_BACKEND=WGPU
          .\Odin\odin check examples/hello %FLAGS% -define:GPU_BACKEND=Dummy -define:PLATFORM_BACKEND=Dummy
